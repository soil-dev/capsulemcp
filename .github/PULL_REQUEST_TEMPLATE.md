<!--
Thanks for the PR. Short is fine — please cover the points below so
reviewers can focus on the diff itself. See CONTRIBUTING.md for the
pre-PR command list.
-->

## Summary

<!-- One-paragraph "what changed and why". For doc-only changes, this
alone is enough — delete the sections below. -->

## What changed

<!-- Bullets are fine. For a new tool: name, Capsule endpoint, an
example call. For a bug fix: what was wrong, what's right now. -->

-

## Test plan

<!-- What you ran locally. Tick what applies; leave the rest. -->

- [ ] `npm run typecheck`
- [ ] `npm run lint` and `npm run format:check`
- [ ] `npm test`
- [ ] `npm run build`
- [ ] Manual exercise (describe below)

<!-- For write-side tool changes, please also exercise the wire-trace
script (`scripts/wire-trace.ts`) against a real Capsule tenant — see
HOWTO.md "Wire-trace against a real Capsule tenant". -->

## Notes for the reviewer

<!-- Anything non-obvious about the approach, alternatives you ruled
out, follow-ups you'd like to defer. Optional. -->
